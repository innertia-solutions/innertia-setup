services:
  api:
    build:
      context: ./api
      dockerfile: docker/prod/Dockerfile
    restart: unless-stopped
    environment:
      APP_ENV: "production"
      APP_DEBUG: "false"

  database:
    restart: unless-stopped

  redis:
    restart: unless-stopped
