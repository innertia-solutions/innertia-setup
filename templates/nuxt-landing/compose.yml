services:
  frontend:
    build:
      context: .
      dockerfile: docker/dev/Dockerfile
    working_dir: /app
    volumes:
      - ".:/app"
      - node_modules:/app/node_modules
    ports:
      - "{{FRONTEND_PORT}}:3000"
    env_file:
      - .env

volumes:
  node_modules:
