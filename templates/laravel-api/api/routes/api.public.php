<?php

use Illuminate\Support\Facades\Route;

// ── Rutas públicas (sin apikey) ────────────────────────────────────────────────
// Route::get('status', fn () => response()->json(['status' => 'ok']));
